let ism = prompt("ismigizni kriting") 
let id= prompt("spaceid yigizni kiritin") 
let password= prompt("spacePasswordgzni kriting")
let mirkomilobj = {
    name: ism,
    id: id,
    password: password
};

console.log(mirkomilobj);
